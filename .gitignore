#### Data files
# Ignore only the original heavy download files; everything else must be tracked.
online_retail*.xlsx
online_retail*.csv
data/raw/online_retail*.xlsx
data/raw/online_retail*.csv

#### Model files
# Track models because trained .pkl files are a required deliverable.
# (So we REMOVE *.pkl and *.h5 from ignore)
# *.pkl
# *.h5

#### Python
__pycache__/
*.pyc
.ipynb_checkpoints/

#### Environment
venv/
.env

#### OS
.DS_Store

#### Logs (from data cleaning and app)
logs/
*.log

#### Docker & compose (must be tracked; just listed for clarity)
# Do NOT ignore these; Docker is required for evaluation.
# Dockerfile
# docker-compose.yml

#### Project structure directories (must be tracked)
# These are mentioned explicitly in the instructions as required:
# src/, notebooks/, docs/, app/, data/processed/, models/
# So we do NOT ignore them at all.
